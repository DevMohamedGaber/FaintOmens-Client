using UnityEngine;
using UnityEngine.UI;
using UnityEngine.Events;
public partial class UIEventsInvite : MonoBehaviour {
    /*public static UIEventsInvite singleton;
    public GameObject panel;
    public Image image;
    public Text Title;
    public Text msg;
    public Button Join;
    public Button Close;

    void Start() => singleton = this;

    public void Show(string title, string message, UnityAction onConfirm, UnityAction onCloseAction = null) {
        Title.text = title;
        msg.text = message;
        Join.onClick.SetListener(onConfirm);
        UnityAction onClose = onCloseAction == null ? () => close() : onCloseAction;
        Close.onClick.SetListener(onClose);
        panel.SetActive(true);
    }
    public void close() {
        panel.SetActive(false);
    }*/
}